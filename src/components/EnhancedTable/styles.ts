import { makeStyles } from '@mui/styles';

export default makeStyles({
  box: {
    width: '100%',
  },
  tableContainer: {
    height: '80vh',
    overflowY: 'scroll',
  },
  tableRow: {
    height: 55,
  },
});