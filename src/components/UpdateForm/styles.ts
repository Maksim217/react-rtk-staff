import { makeStyles } from '@mui/styles';

export default makeStyles({
  updateForm: {
    padding: 15, 
    width: '320px',
  },
  emptyCard: {
    width: 320,
    height: 400,
    objectFit: 'cover',
  },
});